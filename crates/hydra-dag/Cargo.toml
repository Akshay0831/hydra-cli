[package]
name = "hydra-dag"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true, features = ["full"] }
anyhow = { workspace = true }
async-trait = "0.1"
tokio-util = "0.7"
futures = "0.3"

[dev-dependencies]
tokio-test = "0.4"

[features]
default = []
integration-tests = []
